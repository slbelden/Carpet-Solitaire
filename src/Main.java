/** * Displays a deck of cards in order on a pale green background. * @author Stephen Belden * @version 2015.01.30 */import java.awt.Color;import java.awt.Dimension;import java.awt.GridLayout;import javax.swing.ImageIcon;import javax.swing.JFrame;import javax.swing.JPanel;import javax.swing.SpringLayout;import javax.swing.WindowConstants;public class Main {	//Everything is done in the main function.    public static void main(String[] args) {    	//define the size of various things        final int CARD_WIDTH = 73;        final int CARD_HEIGHT = 97;    	final int CARD_GAP = 10;    	final int BORDER = 10;    	    	//create arrays to store cards    	final ImageIcon[] deck = new ImageIcon[52];    	final JPanel[] table = new cardPanel[52 + 4];    	    	//load blank gray cards    	final ImageIcon gray = new ImageIcon("cardImages/gray.gif");    	for(int i = 0; i < 4; i++) {    		table[14 * i] = new cardPanel(gray);    	}    	    	//load card images into cardPanel objects    	final String filepathStub = "cardImages/";    	final String fileExtention = ".gif";    	final String[] numbers = {"ace", "two", "three", "four", "five", "six", "seven", "eight", "nine", "ten", "jack", "queen", "king"};    	final String[] suits = {"Spades", "Hearts", "Diamonds", "Clubs"};    	    	for(int i = 0; i < 4; i++) {    		for(int j = 0; j < 13; j++) {    			String path = ((filepathStub.concat(numbers[j])).concat(suits[i])).concat(fileExtention);    			deck[(13 * i) + j] = new ImageIcon(path);    			table[(14 * i) + (j + 1)] = new cardPanel(deck[(13 * i) + j]);    		}    	}        //create a JFrame to contain the cards        JPanel center = new JPanel();        center.setOpaque(false);        center.setPreferredSize(new Dimension(14 * (CARD_WIDTH + CARD_GAP), 4 * (CARD_HEIGHT + CARD_GAP)));        center.setLayout(new GridLayout(4, 14));        for(int i = 0; i < 56; i++) {        	center.add(table[i]);        }                //create a window to display the card panel and the border around the cards        JFrame window = new JFrame("Cards");        Color paleGreen = new Color(100,200,100);        SpringLayout layout = new SpringLayout();        window.setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);        window.getContentPane().setBackground(paleGreen);        window.setPreferredSize(new Dimension((14 * (CARD_WIDTH + CARD_GAP)) + (BORDER * 2) + 5, (4 * (CARD_HEIGHT + CARD_GAP)) + (BORDER * 2) + 30));        window.setLayout(layout);        layout.putConstraint(SpringLayout.WEST, center, BORDER, SpringLayout.WEST, window);        layout.putConstraint(SpringLayout.NORTH, center, BORDER, SpringLayout.NORTH, window);        window.add(center);        window.pack();        window.setVisible(true);    }}